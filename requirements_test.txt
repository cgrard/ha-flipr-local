# Test dependencies for the Flipr Local integration.
#
# pytest-homeassistant-custom-component pulls in Home Assistant core, pytest,
# pytest-asyncio and the test helpers needed to import the integration.
pytest-homeassistant-custom-component

# Importing the integration package runs __init__.py, which imports
# homeassistant.components.bluetooth (declared as a dependency in manifest.json).
# That component — and the usb component it pulls in — require the following
# third-party libraries. Home Assistant installs them automatically at runtime;
# in CI we must provide them explicitly. Versions mirror the HA pins for the
# Home Assistant release shipped by pytest-homeassistant-custom-component above;
# bump them together when upgrading Home Assistant.
bleak==0.22.3
bleak-retry-connector==3.6.0
bluetooth-adapters==0.20.2
bluetooth-auto-recovery==1.4.2
bluetooth-data-tools==1.20.0
dbus-fast==2.24.3
habluetooth==3.7.0
pyserial==3.5
pyudev==0.24.1
